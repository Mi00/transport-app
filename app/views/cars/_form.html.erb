<%= form_for(car) do |f| %>
<% if car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
      <% car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Nr rejestracyjny" %>
    <%= f.text_field :car_number, placeholder: "np. ZS 091AV" %>
  </div>

  <div class="actions">
    <%= f.submit 'Stwórz samochód' %>
  </div>
<% end %>
